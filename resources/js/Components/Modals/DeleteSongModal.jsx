import Modal from "../Modal";

export default function DeleteSongModal({show, onClose, song}) {
    return (
        <>
            <Modal show={show} onClose={onClose} backdrop="static">
                <h1>MOTHERFUCKERS!!!</h1>
                <p>TOM HAS NO DICK AT ALL PEOPLE</p>
            </Modal>
        </>
    )
}